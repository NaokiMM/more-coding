# Claude 用ガイド（more-coding）

このドキュメントは、Claude がこのリポジトリで作業する際の前提・ルールをまとめたものです。

## 基本方針
- 可能な限り差分を小さくし、意図が明確な変更だけを提案する
- 不明点は決め打ちせず、必要な確認質問を先に行う
- 返答は日本語で行う（コードコメントも必要最小限で日本語）
- 実装方針・設計意図を簡潔に説明し、変更理由を明示する

## このリポジトリの前提
- サイト: https://more-coding.com/
- GitHub: https://github.com/NaokiMM/more-coding

### 開発・構成（概略）
- フロント/本体は Next.js
- HTTP バックエンドは Hono（`hono/api`）
  - `GET /health` で死活確認
  - `POST /run` はコード実行用スタブ（実装は別途）

### コーディング手法
- `Top-Down Readability（トップダウン設計）` を優先する
  - ファイルを開いた時点で「何をするコードか」を即座に把握できる構成にする
  - 実装詳細・補助処理は後回しにして読み進めやすくする

## 環境変数・設定値ルール
- 設定値（URL など）はコードに直接書かず、環境変数で管理する
- `.env.local` をソースにしない（※Gitに含めない）
- Next.js 起動時に `.env.local` が読み込まれ、`process.env` で参照できる前提で設計する

## テスト方針
- 単体テスト: ロジック単位の検証は自動テストで行う
- 結合テスト: API と DB、フロントエンドと API の連携を検証する
- システムテスト（E2E）: `Playwright` で重要なユーザーフロー（認証、サブスクリプション処理など）を中心に自動化する

## GitHub 運用ルール（共同開発）
- `1 push = 1 機能追加` を原則にする（内容を第三者に説明できる形にまとめる）
- main への直接 push は不可。作業は以下の流れで行う
  - 作業用ブランチ作成
  - コミット・push
  - Pull Request 作成
  - 最低1件のレビュー承認後にマージ

## Secrets / 取り扱い注意
- `.env.local` 等のシークレットはコードやコミットに含めない
- Claude がコード差分を提案する際は、秘密情報をベタ書きしない

## 作業時の出力フォーマット（推奨）
- まず「変更の要点（なぜそれが必要か）」を短く提示
- 次に「実装概要（どこをどう変えるか）」を箇条書きではなく短文で説明
- 最後に「テスト観点（どのケースを確認するか）」を明記

